<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.northis.speechvotingapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpeechVotingApp">
        <activity android:name=".view.schedule.ScheduleActivity"></activity>
        <activity android:name=".view.catalog.CatalogActivity" />
        <activity android:name=".view.voting.VotingActivity" />
        <activity android:name=".view.authorization.AuthActivity" />
    </application>

</manifest>